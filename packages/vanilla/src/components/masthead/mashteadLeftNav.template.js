/**
 * Masthead Left Nav template literal
 *
 * @returns {string} ES6 template literal of the horizontal rule
 *
 */
const mastheadLeftNavTemplate = () => {
  return `<p>Masthead Left Nav</p>`;
};

export default mastheadLeftNavTemplate;
