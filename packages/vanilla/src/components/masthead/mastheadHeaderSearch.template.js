/**
 * Masthead Header Search
 *
 * @returns {string} ES6 template literal of the Masthead Header Search
 *
 */
const mastheadHeaderSearchTemplate = () => {
  return `mastheadHeaderSearchTemplate`;
};

export default mastheadHeaderSearchTemplate;
